const greet = (count)=>{
    for (let index = 0; index < count; index++) {
        console.log("Hi there");
        
    }
}
greet(4);

const square =(num)=> num*num;      // 🙇‍♂️🙇‍♂️ Can't write return because it provide only single expression.
console.log(square(4));